# No external dependencies required.
# The CLI agent (agent/agent.py) uses only Python standard library.
# Python 3.8+ is sufficient.

# To run the web UI locally (index.html needs to fetch tree/reflection-tree.json):
#   python -m http.server 8000
# Then open: http://localhost:8000
